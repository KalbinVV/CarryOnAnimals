package org.kalbinvv.carryonanimals.configuration;

public enum ConfigurationLoadType {
	// Strings, booleans, another configuration values
	Paths,
	// Protections, entities lists, worlds list. 
	// Also reload paths.
	Modules
}
